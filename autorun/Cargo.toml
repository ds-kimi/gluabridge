[package]
name = "autorun"
version = "1.2.5"
authors = ["Vurv78 <Vurv78@users.noreply.github.com>"]
edition = "2021"
repository = "https://github.com/Vurv78/Autorun-rs"
publish = false

[lib]
crate-type = ["cdylib", "rlib"]

[features]
default = ["executor", "inject", "plugins", "colors", "http", "ipc"]

executor = [] # Executor inside console
inject = [] # Allow injection through DllMain export
plugins = []
colors = [] # Colors in external console
http = ["tinyget"] # To check for updates and download plugins in the future.
ipc = ["serde_json"] # Local socket server for editor integrations (VSCode extension)

[dependencies]
autorun-shared = { path = "../autorun-shared" }
rglua = { version = "3.0.0" }

detour = { version = "0.8", default-features = false }

# Error handling
thiserror = "1.0"
fs-err = "2.7"

# Settings
serde = { version = "1.0", features = ["derive"] }
toml = "0.5"
serde_json = { version = "1.0", optional = true }

# Global Mutable Variables
once_cell = "1.10"

# Misc
home = "0.5"
libloading = "0.7" # For Autorun.requirebin

# Console
time = "0.3"
colored = "2"

# Windows specific
[target.'cfg(windows)'.dependencies]
trayicon = "=0.1.3"
winapi = { version = "0.3", features = ["wincontypes", "wincon", "processenv", "consoleapi"] }
tinyget = { version = "1.0", features = ["https"], optional = true }

[target.'cfg(unix)'.dependencies]
winit = "0.26"

[build-dependencies]
cfg_aliases = "0.1"
